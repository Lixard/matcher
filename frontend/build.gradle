plugins {
    id "java"
    id "com.moowork.node" version "$node_plugin_ver"
}

node {
    version = "$node_ver"
    npmVersion = "$npm_ver"
    download = true
}

jar.dependsOn "npm_run_build"

jar {
    from "frontend" into "static"
}
